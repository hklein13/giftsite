# Time tracker data (local only)
.timetrack.json

# Git worktrees (isolated workspaces)
.worktrees/

node_modules/
dist/
js/theatre-state.json

# Dev screenshots and temp media (keep assets/ tracked)
/*.png
/*.jpg
/*.jpeg
/*.mp4
/nul

# Playwright MCP cache
.playwright-mcp/

# Viability test page (dev only)
test-viability.html

# Claude local config
.claude/
